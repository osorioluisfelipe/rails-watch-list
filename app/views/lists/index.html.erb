<div class="container">
  <h1>All Lists</h1>

  <% @lists.each do |list| %>
  <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(/Users/luisfelipe/code/osorioluisfelipe/rails-watch-list/app/assets/images/<%=list.name.downcase%>.jpg)">
  <%= link_to "#{list.name}", list_path(list) %>
  </div>
  <% end %>
  <p><%= link_to "New List", new_list_path, class: 'btn btn-primary' %></p>
</div>

<%= image_tag "action.jpg", alt: "Le Wagon" %>
